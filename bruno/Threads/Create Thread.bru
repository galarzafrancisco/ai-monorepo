meta {
  name: Create Thread
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}{{apiPrefix}}/threads
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Bruno API exploration",
    "parentTaskId": "{{taskId}}"
  }
}
