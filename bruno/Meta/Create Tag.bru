meta {
  name: Create Tag
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}{{apiPrefix}}/meta/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "{{tagName}}"
  }
}
